---
- hosts: localhost
  become: yes
  connection: ssh

  tasks:
    - name: install httpd apache webserver
      command: sudo install httpd
      notify: restart httpd

  handlers:
    - name: restart httpd
      service: name=httpd state=restarted
